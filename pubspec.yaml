name: flutter_application_1
description: A new Flutter project.
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: "none" # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ">=2.19.6 <3.0.0"

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter
  http: ^0.13.4
  video_player: ^2.1.6
  flutter_svg:

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^2.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:
  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
    - images/SVG/weltkugel.svg
    - images/navigation_icon_001.png
    - images/navigation_icon_002.png
    - images/navigation_icon_003.png
    - images/background.jpg
    - images/back.png
    - images/placeholder.png
    - images/der_agile_professor.png
    - images/der_aktive.png
    - images/der_alles_an_sich_ziehende.png
    - images/der_auf_und_abbau.png
    - images/die_bewusste_wandlung.png
    - images/das_chaotische_glueck.png
    - images/der_denker.png
    - images/der_dirigent_des_lebens.png
    - images/die_dummheit.png
    - images/der_durchstarter.png
    - images/der_energetische_fluss.png
    - images/der_entmachtete.png
    - images/der_emotionale_sicherheitgeber.png
    - images/der_eroberer.png
    - images/der_flexible.png
    - images/der_gefuehlsdisziplinierte.png
    - images/der_gefuehlsdirektor.png
    - images/der_gefuehlsmensch.png
    - images/der_gefuehlvolle_sprinter.png
    - images/der_gefuehlvolle_stuermer.png
    - images/der_gesicherte_erfolg.png
    - images/der_geteuerte_weitblick.png
    - images/die_glueckseligkeit.png
    - images/das_glueckskind.png
    - images/der_glueckspilz.png
    - images/der_großunternehmer.png
    - images/der_haeuslebauer.png
    - images/die_herzensbrecherin.png
    - images/die_inspiration.png
    - images/der_instabile.png
    - images/die_irrefuehrung.png
    - images/der_kampf_zwischen_gefuehl_und_trophaee.png
    - images/das_kopf-gefuehlschaos.png
    - images/das_kreative_genie.png
    - images/der_kreative_kuenstler.png
    - images/der_kuenstler_im_absoluten.png
    - images/die_logische_beziehung.png
    - images/der_logische_energieeinsatz.png
    - images/der_logische_kalkulator.png
    - images/das_materialliserte_glueck.png
    - images/der_partnerwechsel.png
    - images/der_powerstart.png
    - images/der_powerstart.png
    - images/die_sichtbare_stabilitaet.png
    - images/der_spirituelle_alchemist.png
    - images/der_stabile_greis.png
    - images/der_stabile.png
    - images/der_stolze_king_of_life.png
    - images/der_stolze_repraesentant.png
    - images/der_traeumer.png
    - images/die_traeumerische_sichtweise.png
    - images/der_unbewegliche.png
    - images/der_unflexible.png
    - images/der_ungeliebte.png
    - images/der_unglueckliche.png
    - images/der_unsichere.png
    - images/der_verblendete.png
    - images/die_versuchung.png
    - images/der_verwandlungskuenstler.png
    - images/der_visuelle_chaot.png
    - images/der_wandler.png
    - images/der_wissenschftliche_gluecksbringer.png
    - images/der_zielbewusste_neureiche.png
    - images/appbar_background.png
    - videos/splash.webm
    - videos/splash_mobile_small.mp4
    - videos/splash_mobile.mp4
    - videos/splash_desktop.mp4
    - lang/deDE.json
    - lang/enEN.json
    - lang/arAR.json
    - lang/frFR.json
    - lang/ruRU.json
    - lang/trTR.json
    - lang/zhZH.json
    - lang/itIT.json
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/assets-and-images/#resolution-aware

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/assets-and-images/#from-packages

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/custom-fonts/#from-packages
